
Процедура Печать(ТабДок, Ссылка) Экспорт
	Перем СоответствиеОбъектовИМакетов;
	
	//{{_КОНСТРУКТОР_ПЕЧАТИ(Печать)
	Макет = Справочники.Канцтовары.ПолучитьМакет("Печать");
	Запрос = Новый Запрос;
	Запрос.Текст =
	"ВЫБРАТЬ
	|	Канцтовары.Ссылка,
	|	Канцтовары.Код,
	|	Канцтовары.Комментарий,
	|	Канцтовары.НаименованиеДляПечати,
	|	Канцтовары.Производитель
	|ИЗ
	|	Справочник.Канцтовары КАК Канцтовары
	|ГДЕ
	|	Канцтовары.Ссылка В (&Ссылка)";
	Запрос.Параметры.Вставить("Ссылка", Ссылка);
	Выборка = Запрос.Выполнить().Выбрать();

		ТабДок.Очистить();

	ВставлятьРазделительСтраниц = Ложь;
	//Пока Выборка.Следующий() Цикл
	//	Если ВставлятьРазделительСтраниц Тогда
	//		ТабДок.ВывестиГоризонтальныйРазделительСтраниц();
	//	КонецЕсли;

	//	ТабДок.Вывести(ОбластьЗаголовок);

	//	Шапка.Параметры.Заполнить(Выборка);
	//	ТабДок.Вывести(Шапка, Выборка.Уровень());

	//	ВставлятьРазделительСтраниц = Истина;
	//КонецЦикла;
	//}}
//КонецПроцедуры


СоответствиеОбъектовИМакетов = Новый Соответствие;
СоответствиеОбъектовИМакетов.Вставить("Текстовый ключ");
СтруктураОбъектовИМакетов = Новый Структура;

СтруктураОбъектовИМакетов.Вставить("ТекстовыйКлюч1");
СтруктураОбъектовИМакетов.Вставить("ТекстовыйКлюч2");
СтруктураОбъектовИМакетов.Вставить("ТекстовыйКлюч3");

НужнаяПеременная = Неопределено;
ЕстьСвойтсво = СтруктураОбъектовИМакетов.Свойство("ТекстовыйКлюч1", НужнаяПеременная);

Для Каждого ЭлементМассива из Ссылка Цикл
	СоответствиеОбъектовИМакетов.Вставить(ЭлементМассива, Справочники.Канцтовары.ПолучитьМакет("Печать"));
	Если ЭлементМассива.ЭтоГруппа Тогда 
		СоответствиеОбъектовИМакетов.Вставить(ЭлементМассива, Справочники.Канцтовары.ПолучитьМакет("ПечатьГруппа"));
	КонецЕсли;
КонецЦикла;

//Ссылка.Добавить

Для Сч = 1 по Выборка.Количество() Цикл
	Выборка.Следующий();
	Макет = СоответствиеОбъектовИМакетов.Получить(Выборка.Ссылка);
	ОбластьЗаголовок = Макет.ПолучитьОбласть("Заголовок");
	Шапка = Макет.ПолучитьОбласть("Шапка");
Если ВставлятьРазделительСтраниц Тогда
		ТабДок.ВывестиГоризонтальныйРазделительСтраниц();
		Сообщить("Вывели горизонтальный разделитель");
	Иначе
		Сообщить("Не вывели горизонтальный разделитель");
	КонецЕсли;
	
	ТабДок.Вывести(ОбластьЗаголовок);
	
	Шапка.Параметры.Заполнить(Выборка);
	ТабДок.Вывести(Шапка, Выборка.Уровень());
	
	ВставлятьРазделительСтраниц = Истина;
	Сообщить("Количество выделено " + Выборка.Количество());
	
КонецЦикла;


КонецПроцедуры
